# Cloudflare's date-versioned packages (@cloudflare/workers-types) and wrangler
# bump their peer ranges (and majors) often. Relax npm's peer resolution so a
# fresh install doesn't break every time one of them moves.
legacy-peer-deps=true
